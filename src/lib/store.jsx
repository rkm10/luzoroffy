// import { create } from "zustand";

// export const useAnimeStore = create((set) => ({
//   animeList: [],
//   addAnime: (newAnime) =>
//     set((state) => ({ animeList: [...state.animeList, ...newAnime] })),
// }));
